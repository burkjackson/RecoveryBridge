# RecoveryBridge — Project Status Document

> **Last Updated:** August 2026
> **Purpose:** Get any Claude session up to speed on the entire app — architecture, features, current state, and known issues.

---

## What Is RecoveryBridge?

RecoveryBridge is a **peer-to-peer support platform for people in addiction recovery**. Users can be **seekers** (people needing support) or **listeners** (people offering support). They connect in real-time 1:1 chat sessions. Think of it as an anonymous, on-demand peer support hotline.

**Live URL:** https://recoverybridge.app (Vercel, auto-deploys from `main`)
**Note:** The GitHub repo (`burkjackson/RecoveryBridge`) is **public**.

---

## Tech Stack

| Layer | Technology |
|-------|-----------|
| Framework | Next.js 15 (App Router, React 18, TypeScript) |
| Database | Supabase (PostgreSQL + Auth + Realtime + Storage) |
| Styling | Tailwind CSS 3.4, custom theme, class-based dark mode |
| Push Notifications | web-push (VAPID) + Service Worker |
| Email | Resend (welcome, admin signup alerts, support-request fallback) |
| SMS (disabled) | Twilio (pending verification) |
| Error Tracking | Sentry (@sentry/nextjs) |
| Image Cropping | react-easy-crop |
| Hosting | Vercel |
| Cron | GitHub Actions (`.github/workflows/cron.yml`, every 15 min) + daily Vercel crons as backup |

---

## Project Structure

```
app/
├── api/
│   ├── heartbeat/route.ts            # POST: Update user's last_heartbeat_at
│   ├── cleanup-sessions/route.ts     # POST/GET: Auto-close stale sessions, reset stale seekers + stale-available listeners, expire temp blocks (cron)
│   ├── scheduled-availability/route.ts # POST/GET: Push "your support time is starting" (cron)
│   ├── notifications/send/route.ts   # POST: Push (+email fallback) to available listeners
│   ├── notifications/message/route.ts # POST: Push a new chat message to the other participant (if they aren't looking)
│   ├── notifications/thank-you/route.ts    # POST: Queue a "you got a thank-you note" push (server-verifies the feedback row)
│   ├── notifications/training-nudge/route.ts # POST/GET: Queue nudges for stalled listener training (cron)
│   ├── notifications/reengagement/route.ts   # POST/GET: Queue the opt-in monthly check-in (cron)
│   ├── notifications/drain/route.ts   # POST/GET: Deliver queued notifications, honouring prefs + quiet hours (cron)
│   ├── sessions/state/route.ts       # POST: Move BOTH participants' role_state on session start/end
│   ├── account/export/route.ts       # GET: Self-service data export (CCPA/GDPR)
│   ├── account/delete/route.ts       # POST: Self-service account deletion (service role → auth.admin.deleteUser)
│   ├── contact/route.ts              # POST: Public contact form (IP rate-limited)
│   ├── splash/route.tsx              # GET: Generated iOS PWA splash images (edge)
│   ├── email/welcome/route.ts        # POST: Send welcome email (Resend)
│   ├── webhooks/new-user/route.ts    # POST: Supabase webhook — admin new-user email (superseded by notify-signup; confirm no dashboard webhook still points here before removing)
│   └── admin/
│       ├── actions/route.ts          # POST: All admin mutations (verify token → is_admin, rate-limited)
│       ├── delete-user/route.ts      # POST: Admin user deletion
│       ├── notify-signup/route.ts    # POST: Admin signup-alert email (called from onboarding completion)
│       └── send-welcome-bulk/route.ts # POST: Welcome-email backfill for a NAMED set of recipients
├── admin/page.tsx                    # Admin moderation dashboard (~1,800 lines)
├── chat/[id]/page.tsx                # Real-time 1:1 chat (~1,700 lines)
├── connect/page.tsx                  # Notification-tap landing: verifies seeker, ASKS the listener to confirm, then creates the session → /chat
├── dashboard/page.tsx                # Main hub: role selection, listeners, seekers, notifications
├── history/page.tsx                  # Past sessions + feedback/thank-you notes
├── training/page.tsx                 # Listener training modules (acknowledge sections → completion timestamp)
├── profile/page.tsx                  # Edit profile, tags, avatar, notification/availability settings
├── listeners/page.tsx                # Browse & filter available listeners, connect
├── onboarding/page.tsx               # Post-signup setup (role, bio, tags, referral source, guidelines)
├── login/ signup/ forgot-password/ reset-password/  # Auth pages
├── support/                          # 7 static SEO landing pages (peer support, recovery chat, …) + index
├── contact/ donate/ safety/ terms/ privacy/ offline/ # Static-ish pages
├── support/                          # 7 public SEO landing pages (peer support, recovery chat, etc.)
├── layout.tsx                        # Root layout: PWA metadata, CrisisResources, theme, service worker
├── page.tsx                          # Landing page (FAQ accordion, product preview popup)
└── sitemap.ts                        # Generated sitemap

components/
├── AvailableListeners.tsx            # Real-time list of online listeners (dashboard, connect flow)
├── PeopleSeeking.tsx                 # Real-time list of seekers needing help (for listeners)
├── NotificationSettings.tsx          # Push toggle, Always Available, Quiet Hours, availability schedule
├── NotificationInstructionsModal.tsx # iOS PWA installation guide
├── BottomNav.tsx                     # Mobile bottom navigation
├── ThemeProvider.tsx / ThemeToggle.tsx # Class-based dark mode
├── ToastProvider.tsx                 # Toast notifications
├── FaqAccordion.tsx / ProductPreview.tsx / SocialIcons.tsx # Landing page
├── AvatarUpload.tsx                  # Image upload with crop (Supabase Storage)
├── CrisisResources.tsx               # Floating 988/crisis button (always visible)
├── NoticeBanner.tsx                  # Surfaces unread in-app notices (user_notices) on the dashboard
├── BroadcastComposer.tsx             # Admin: compose/send a platform announcement to a named audience
├── NotificationControls.tsx          # Admin: per-kind on/off switches for what the platform may send
├── TagSelector.tsx / Modal.tsx / ErrorState.tsx / Skeleton.tsx / Footer.tsx
├── ServiceWorkerRegistration.tsx / SkipLink.tsx
└── ui/Typography.tsx                 # Semantic type scale (Heading1..., Body16, Body18)

lib/
├── constants.ts                      # Timing, validation, tags, reactions, timezones, parseReferralSource, outreach + notification copy, broadcast audiences, training sections
├── email.ts                          # Resend senders: support-request fallback, contact form, report-resolved
├── email/welcomeEmailHtml.ts         # Welcome email template
├── email/escapeHtml.ts               # HTML escaping for user-supplied values in emails
├── timeWindows.ts                    # Quiet-hours + availability-window maths (pure, unit-tested)
├── rateLimit.ts                      # Shared in-memory limiter used by every API route
├── blocks.ts                         # getActiveBlock() — ignores lifted AND expired blocks
├── sessionState.ts                   # Client helper → /api/sessions/state
├── serverPush.ts                     # Shared server-side web-push sender (single-user + batched)
├── notificationQueue.ts              # Queue for every non-support push: categories, consent + quiet-hours rules, enqueue
├── cronAuth.ts                       # Shared cron-secret / Bearer auth for the cron API routes
├── favorites.ts                      # normalizeFavorites() — RLS-safe favorite handling (see Known Issues)
├── errors.ts                         # errorMessage() for `catch (e: unknown)`
├── faqs.ts                           # Landing page FAQ content
├── pushNotifications.ts              # Web Push subscribe/unsubscribe
├── linkify.tsx                       # Safe URL autolinking in chat
├── sms.ts                            # Twilio sender (feature disabled)
├── safeRedirect.ts                   # safeRedirectPath() — guards ?redirect= against open-redirect (see Known Issue #39)
├── deleteUserAccount.ts              # deleteUserAccount() — shared by self-service + admin delete routes (see #42)
├── validation.ts                     # UUID_RE — shared user-id format check
├── directConnect.ts                  # startDirectConnect() — seeker-initiated connect, shared by 3 call sites (see #43)
├── acceptSeeker.ts                   # acceptSeeker() — listener accepting a seeker, shared by 2 call sites (see #43)
├── mutes.ts                          # getMutedUserIds() — pairwise mute id set, both directions collapsed (see #49)
├── test/fakeSupabase.ts              # Shared fake Supabase client for the API route tests below, dispatch-by-table (see #50)
├── supabase/client.ts, server.ts
├── types/database.ts
└── *.test.ts                         # Vitest: constants, favorites, timeWindows, rateLimit, errors, safeRedirect, directConnect, acceptSeeker
                                       # + app/api/sessions/state, app/api/notifications/send, app/api/admin/actions route tests (see #50)

supabase/
├── migrations/                       # 001–050, numbered (note: two files share 004; 038 and 046 are NOT yet applied) — see migrations/README.md
└── legacy/                           # Pre-migration setup SQL (historical snapshot; the live policy set has
                                      #   drifted — query pg_policies for the truth, see migrations/README.md)

docs/                                 # Setup guides + dated historical snapshots (each labelled); all intentionally public
scripts/                              # Ad-hoc admin scripts (DEPLOY.sh; get-user-emails.js removed — see Known Issues)
public/sw.js                          # Service worker (push, cache — bump CACHE_NAME on breaking changes; currently v12)
middleware.ts                         # Route protection (auth + admin check)
.github/workflows/cron.yml            # 15-min pings to cron API routes
.github/workflows/ci.yml              # Typecheck, lint, test, build on every push (no secrets needed)
```

---

## Database Schema (high level)

Migrations live in `supabase/migrations/` (001–046) and are the source of truth — but note that a file existing is not evidence it ran; `038` and `046` are written and **not yet applied**. Summary:

### profiles (central user table)
Core: `id` (= auth.users.id), `display_name` (unique), `email`, `bio`, `tagline`, `avatar_url`, `tags` (max 5), `is_admin`.
State: `role_state` (`available`/`requesting`/`offline`/null), `user_role` (`person_in_recovery`/`professional`/`ally`), `last_heartbeat_at`.
Notifications: `always_available`, `quiet_hours_*` (enabled/start/end/timezone), `email_notifications_enabled` (008), `phone_number` + `sms_notifications_enabled` (006, feature disabled), `availability_schedule` JSONB windows (020), `last_availability_notify_key` (027, dedupes the "support time is starting" push — `"YYYY-MM-DD|day|HH:MM"` in the user's timezone), `announcement_notifications_enabled` (035, default **true** — thank-you notes, training nudges, broadcasts) and `reengagement_notifications_enabled` (035, default **false** — the opt-in monthly check-in). Neither governs support-request pushes, which keep the original push toggle.
Training: `listener_training_progress` JSONB + `listener_training_progress_at` (035, per-section acks so progress survives a reload and the nudge cron can tell stalled from in-progress).
Compliance/audit: `referral_source` (010, free text since 018), `listener_training_completed_at` (019), `consent_version` + `consent_accepted_at` (021), `age_confirmed` (022), `health_data_consent` + `_at` (023, WA My Health My Data).

### Other tables
- **sessions** — listener_id, seeker_id, status (`active`/`ended`), ended_at, `accepted_at` (039, applied — NULL means a direct-connect session the listener hasn't accepted; a restrictive RLS policy blocks the seeker from messaging until it's set). A unique partial index (025) enforces **one active session per seeker**, so a duplicate insert fails with `23505` and callers fall back to "someone else just connected"
- **messages** — session_id, sender_id, content (max 2000), read_at (002, read receipts)
- **message_reactions** — 8 emoji types (003/004)
- **session_feedback** — helpful boolean + `thank_you_note` (009, max 300 chars, shown in /history)
- **user_favorites** (007) — favorite contacts from past sessions; favorites get notified first
- **reports / user_blocks / admin_logs** — moderation + audit trail (`user_blocks.expires_at` is honoured by `lib/blocks.ts` and swept by the cleanup cron)
- **user_notices** (026) — in-app messages to a *user*: auto "we couldn't connect you" follow-ups (`kind='reconnect'`), admin outreach (`kind='outreach'`), broadcasts (`kind='announcement'`)
- **push_subscriptions** — Web Push endpoints per user/device
- **notification_queue** (035) — one row per recipient per non-support notification (thank-you note, training nudge, check-in, broadcast). Drained by `/api/notifications/drain`; `dedupe_key` + a partial unique index make every enqueue idempotent, `not_before` doubles as a quiet-hours deferral and a claim lease
- **broadcasts** (035) — audit record for an admin announcement; queue rows hang off it
- **notification_kind_settings** (036) — the admin on/off switch per notification kind. Absent row = off; unreadable = everything off
- **user_notices** (026) — in-app messages to a *user* (not an email): `kind` is `reconnect` (auto follow-up when a seeker never got connected) or `outreach` (personal note from an admin). Surfaced by `NoticeBanner` on the dashboard; the admin "Couldn't Connect" tab reads the `reconnect` rows
- **blog/story tables** (011–015) — **legacy**: stories moved to Ghost at stories.recoverybridge.app; no in-app UI reads them

All tables have RLS. Admin mutations go through `/api/admin/*` routes (Bearer token → `getUser` → `is_admin` check), not client-side Supabase.

---

## Core User Flows

### 1. Seeker requesting support
1. "I Need Support" → `role_state = 'requesting'`, push fires to available listeners
2. Seeker stays on dashboard with "Finding Listener..."; heartbeat every 30s
3. Re-notification after 2 min without connection (max 3)
4. Listener connects (PeopleSeeking, dashboard list, /listeners, or notification tap → `/connect?seekerId=`). **Every path confirms first** — the in-app lists via a modal, the notification tap via the /connect page. Nothing is created until a listener explicitly says they're ready, so a stray tap can't consume a seeker
5. Seeker auto-navigates to `/chat/[sessionId]` via realtime subscription

### 2. Listener flows
- "I'm Here To Listen" → `role_state = 'available'`, visible in lists
- Seekers can also directly connect from the dashboard listener list, /listeners, or Favorites; the listener gets a distinct "🎯 Direct Connection Request" push/email. The session exists and the seeker is dropped into /chat immediately, but it starts **pending** (`sessions.accepted_at IS NULL`, migration 039) — the composer is hidden for both sides and the seeker cannot send a message (enforced by a restrictive RLS policy, not just the UI) until the listener taps Accept in chat. See Known Issue #28. The listener's own `role_state` is untouched while pending — only the seeker moves to `offline` at the start of a direct connect; the listener only leaves the pool once they tap Accept (see Known Issue #35). A pending session still blocks a second direct-connect to the same listener (migration 029's one-active-session-per-listener index doesn't distinguish pending from accepted) — acceptable since the cleanup cron closes an unanswered pending session after 10 minutes.
- Listener training (/training) nudged from dashboard; completion recorded on profile
- Optional weekly availability schedule → "your support time is starting" push at window start

### 3. Chat session
- Realtime messages (postgres_changes), typing indicators + read receipts (broadcast), reactions (double-click, 8 types), URL autolinking, conversation starters, crisis-language banner
- Inactivity: warn at 15 min, auto-close 5 min later; either party can end → feedback modal (helpful? + optional thank-you note) → favourite prompt → dashboard
- Session start/end moves participants' `role_state` via `/api/sessions/state` (see Known Issue #26) — both sides on end; on start, both sides for an already-accepted session, but only the seeker for a still-pending direct connect (see Known Issue #35)
- Report flow (3-step) available in chat

### 4. Notification system (in priority order)
1. **Push** (VAPID web-push): favorites of the seeker get a personalized push first, general listeners 4s later; invalid subscriptions (4xx) auto-removed. Favourites are resolved **server-side** from `user_favorites` — the client's list is RLS-filtered and silently omits offline favourites
2. **Email fallback** (Resend): listeners who opted in (`email_notifications_enabled`) and didn't get a successful push
3. **SMS fallback**: fully coded but disabled pending Twilio verification
- Server-side quiet-hours filtering (listener's local time); rate limit 3 req/60s per user (in-memory — see Known Issues)
- Targets: `role_state = 'available'` OR `always_available = true`

**Separate from the above:** `/api/notifications/message` pushes a *new chat message* to the other participant of an active session, suppressed if they're already looking at the chat (server checks whether the message got marked read; the service worker also suppresses on window visibility). Deliberately skips quiet hours — it's a live conversation the recipient already opted into.

**Everything that isn't a support request goes through the queue** (`notification_queue`, migration 035), not an inline send:

| Message | Category | Queued by | Governed by |
|---------|----------|-----------|-------------|
| You received a thank-you note | `announcement` | `/api/notifications/thank-you`, from the post-session feedback modal | `announcement_notifications_enabled` (default on) |
| Finish your listener training | `announcement` | `/api/notifications/training-nudge` (cron) | same; only for people who started and stalled 3+ days |
| Admin broadcast | `announcement` | `send_broadcast` admin action | same; also writes a `user_notices` row so it lands without push |
| "It's been a while" check-in | `reengagement` | `/api/notifications/reengagement` (cron) | `reengagement_notifications_enabled` (default **off**, opt-in) |
| "Your availability was paused" check-in | `announcement` | `cleanup-sessions` cron, to the exact profiles `resetStaleAvailability()` just flipped to `offline` | same; migration 037 |

**Every kind is behind an admin switch** (`notification_kind_settings`, migration 036, +037 for `listener_checkin`), separate from the per-recipient consent above — that one asks "does this person want it", this one asks "do we want it going out at all yet". Both must say yes. The four automatic kinds ship **off**; `broadcast` ships **on** because it cannot fire without an admin composing and sending. Managed in Admin → Notifications.

The switch is enforced in three places, and all of them matter: `enqueueNotifications` won't create rows for a disabled kind, the drain re-checks at delivery (so switching off *cancels* an existing backlog rather than letting it drain), and `send_broadcast` checks before writing its `user_notices` rows. It fails closed — an unknown kind, or an unreadable settings table, sends nothing.

`/api/notifications/drain` delivers them on the shared cron: it re-checks consent at send time, **defers** anything landing in the recipient's quiet hours (moves `not_before` forward rather than dropping it), retires rows past `expires_at`, and prunes terminal rows after 30 days.

Three rules worth keeping:
1. **Never fold these into the support-request toggle.** Someone who mutes push to escape an announcement also stops hearing that a person needs support. That's why the categories are separate preferences and why support requests keep the original toggle.
2. **Push text renders on a locked phone.** Nothing in `NOTIFICATION_COPY` names the other person, quotes what they wrote, or implies why the recipient uses RecoveryBridge.
3. **Don't bake a live count into a queued body.** The check-in deliberately says "listeners are around", not "3 listeners are online" — the queue can drain 35+ minutes later (longer through quiet hours), by which time a number is a lie.

### 5. Admin moderation (/admin)
Tabs for reports, blocks, sessions, users, sign-ups (with referral source), "Couldn't Connect", and Broadcast (`components/BroadcastComposer.tsx` — pick a named audience, preview its size and how many can receive a push, send) (`missed` — seekers whose request went unanswered, from `user_notices`; an admin can send them a personal note). All mutations via `/api/admin/actions` (server-verified `is_admin`, rate-limited, audit-logged including transcript views).

---

## Cron Jobs (fixed July 2026)

**Primary trigger: GitHub Actions** (`.github/workflows/cron.yml`) — every 15 minutes, pings:
- `POST /api/scheduled-availability` with `x-cron-secret` — notifies listeners whose availability window started within the last 90 min (wide because the cron cadence is unreliable), skipping anyone whose `last_availability_notify_key` already marks that occurrence, and never firing past the window's own end
- `POST /api/cleanup-sessions` with `x-cleanup-secret` — closes empty (>10 min) and inactive (>30 min) sessions, resets stale requesting seekers, and records a `reconnect` notice + warm push for any seeker whose request went unanswered. **Unanswered means the listener never replied**, not merely that no session row exists — see known issue #24
- `POST /api/notifications/training-nudge` with `x-cron-secret` — queues a nudge for anyone who started listener training and stalled 3+ days; monthly dedupe key
- `POST /api/notifications/reengagement` with `x-cron-secret` — queues the opt-in monthly check-in, and only when a listener is genuinely online
- `POST /api/notifications/drain` with `x-cron-secret` — **runs last**, so anything the two steps above queued goes out on the same tick. Delivers up to 200 queued rows, honouring category consent and quiet hours

Requires GitHub repo secret `CLEANUP_SECRET_KEY` (same value as Vercel env var). Daily Vercel crons in `vercel.json` remain as backup; both routes also accept `Authorization: Bearer <CLEANUP_SECRET_KEY|CRON_SECRET>` and answer GET (Vercel crons send GET). Cron-only (fixed 2 Sep 2026, see #36) — the dashboard used to also trigger it on every page load with any signed-in user's token; that path is gone.

---

## Key Constants

See `lib/constants.ts` (source of truth). Highlights: heartbeat 30s ping / 1h online threshold (5 min for PeopleSeeking freshness), inactivity warn 15 min + close 5 min later, re-notify every 2 min max 3×, seeker requesting state goes stale after 30 min, message max 2000 chars, 18 specialty tags (max 5), 8 reaction emoji, 44 timezones (worldwide — US, Canada, Latin America, Europe, Middle East/Africa, Asia-Pacific), `parseReferralSource()` helper.

---

## Environment Variables

### Required
```
NEXT_PUBLIC_SUPABASE_URL / NEXT_PUBLIC_SUPABASE_ANON_KEY / SUPABASE_SERVICE_ROLE_KEY
VAPID_PUBLIC_KEY / VAPID_PRIVATE_KEY / NEXT_PUBLIC_VAPID_PUBLIC_KEY / VAPID_SUBJECT
CLEANUP_SECRET_KEY                 # Cron auth (also a GitHub Actions secret)
RESEND_API_KEY                     # Email (welcome, fallback notifications, admin alerts)
```

### Optional
```
CRON_SECRET                        # Alternate cron secret (Vercel sends as Bearer if set)
NEXT_PUBLIC_SITE_URL               # Auth redirect base (signup confirm, password reset); falls back to window.location.origin
ADMIN_NOTIFICATION_EMAIL / SUPABASE_WEBHOOK_SECRET   # new-user webhook route
NEXT_PUBLIC_SENTRY_DSN / SENTRY_ORG / SENTRY_PROJECT / SENTRY_AUTH_TOKEN
TWILIO_ACCOUNT_SID / TWILIO_AUTH_TOKEN / TWILIO_PHONE_NUMBER   # SMS (disabled)
```

---

## Middleware & Auth

`middleware.ts` protects `/dashboard`, `/chat`, `/profile`, `/listeners`, `/training`, `/history` (→ `/login?redirect=`), `/onboarding` (→ `/signup`), and `/admin` (requires `is_admin`, else → `/dashboard`). Uses `@supabase/ssr` cookie sessions.

---

## Design System

- Brand colors (`tailwind.config.js`): rb-dark #2D3436, rb-gray #4A5568, rb-blue #4A6A7C (light #E8EEF2, hover #3A5A6C, dark #2E4754), rb-purple #B8A9C9 (light #E8E4F0) — all WCAG AA
- Semantic type scale via `components/ui/Typography.tsx` and heading CSS classes
- Class-based dark mode (ThemeProvider/ThemeToggle); dim greys bumped to gray-300 for legibility
- Button hierarchy: primary = solid rb-blue, secondary = light outline, destructive = red
- `prefers-reduced-motion` respected globally; 44px touch targets; skip link; crisis button always visible
- PWA: installable, standalone, start_url /dashboard; iOS needs Add to Home Screen before enabling push

---

## Current Feature Status

Everything in the flows above is ✅ live, including: auth, onboarding (with referral source + consent capture), real-time chat (reactions, read receipts, typing, linkify, crisis banner), push + email notifications, new-message pushes, favorites-first notification priority, quiet hours, always-available, re-notifications, listener directory, listener training, availability schedules, session history + thank-you notes, feedback, reporting, admin dashboard (incl. sign-ups and "Couldn't Connect" tabs), in-app notices, public contact form, SEO support pages, account deletion (server-side, `/api/account/delete`) + data export, PWA, dark mode, Sentry, session cleanup.

| Feature | Status | Notes |
|---------|--------|-------|
| SMS fallback | 🔇 Disabled | Code complete; Twilio verification pending (see below) |
| In-app blog/stories | 🚚 Moved | Now on Ghost (stories.recoverybridge.app); DB tables 011–015 are legacy |
| GitHub Actions cron | ✅ Running | 1,000+ consecutive successful runs; needs the `CLEANUP_SECRET_KEY` repo secret to stay set or runs 401 |
| In-app notices | ✅ Live | Auto follow-up after a failed connection + admin outreach (`user_notices`, `NoticeBanner`) |
| Unread-message push | ✅ Live | `/api/notifications/message` — pushes the other participant only when they aren't looking at the chat |
| SEO support pages | ✅ Live | Seven static pages under `/support`, listed in `app/sitemap.ts` |
| Thank-you-note push | ✅ Live | Queued from the feedback modal; server re-reads the note from `session_feedback` |
| Training nudge push | ✅ Live | Only for people who started training and stalled 3+ days; monthly cap |
| Re-engagement check-in | ✅ Live | Opt-in (default off), monthly cap, only sent when listeners are online |
| Admin broadcast | ✅ Live | Admin → Broadcast tab; push + in-app notice to a named audience |
| Notification categories | ✅ Live | Announcements (default on) and check-ins (opt-in), separate from support-request push |
| Admin send switches | ✅ Live | Admin → Notifications. Automatic kinds ship **off**; flipping one off also cancels its queued backlog |

---

## Known Issues & Technical Debt

1. **Migrations 020 and 027–037, plus 039, are applied** (24–29 Aug 2026) — admin-flag protection, one-active-session-per-listener, session-participant validation, temporary-block expiry, the `role_state='requesting'` SELECT policy, the availability-notify dedupe key, profile visibility for people you already know (033), the notification log (034), the notification queue with its per-category consent (035), the per-kind admin send switches (036, +037 for `listener_checkin`), and — long overdue — `020_availability_schedule`, which had never been run. `039` (`sessions.accepted_at` + the restrictive messages RLS policy behind the direct-connect accept gate, see #28) applied 29 Aug. See `supabase/migrations/README.md` for what each does and how it was verified. `032` fixed a live outage: no non-admin listener could see anyone requesting support, so People Seeking was empty and every notification tap said "This person is no longer waiting for support". **`038` is written but NOT applied** — it drops `profiles.requesting_since` (never written or read) and the orphaned `blocks` table (0 rows, superseded by `user_blocks`). Paste it into the Supabase SQL editor to apply; nothing in the app depends on either object, so there is no deploy ordering to worry about. **`040` and `041` are applied** (2 Sep 2026) — `040` revokes table-level SELECT and grants back only the public columns (see gotchas #19 and #20) and adds `get_my_private_profile()`, closing the email-leak hole; `041` fixes blocking enforcement (see #29). **`042` is applied** (2 Sep 2026) — it replaces the `avatars` storage bucket's open `FOR ALL` policy and the leftover anon-upload template policies with ownership-scoped INSERT/UPDATE/DELETE and closes the open `blog-images` upload policy; see #30 for the size-limit correction. **`043` and `044` are applied** (2 Sep 2026) — `043` corrects 042's avatar size limit to 5 MB (see #30); `044` drops the `reports` SELECT policy that let a reported user read the report filed against them (see #31). **`045` is applied** (2 Sep 2026) — function-hygiene and live-DB-drift cleanup (adds `protect_session_transitions()` to the repo, fixes six missing `search_path`s, revokes RPC access from trigger functions, drops the dead `cleanup_stale_availability()`) — see #38 and `supabase/migrations/README.md` (045). **`046` is written but NOT applied** — dedupes 13 RLS policies and wraps `auth.uid()` as `(select auth.uid())` across ten tables — see #40 and `supabase/migrations/README.md` (046) before pasting it in. **`047` is applied** (3 Sep 2026) — moves consent capture into `handle_new_user()` and adds a `protect_training_completion` trigger so `listener_training_completed_at` can only be set once every training section is acknowledged — see #44 and `supabase/migrations/README.md` (047). **`048` is applied** (3 Sep 2026) — revokes RPC access on `protect_training_completion()`, missed by 045 since it did not exist yet — see `supabase/migrations/README.md` (048). **`049` is applied** (3 Sep 2026) — covers `messages.sender_id`, `reports.reporter_id`, `reports.reported_user_id`, and `session_feedback.from_user_id` with plain b-tree indexes; each one backs a real `.eq()` filter in `app/api/account/export/route.ts` or a real embed join, checked against the code rather than trusting the advisor's FK list at face value — see #48 and `supabase/migrations/README.md` (049). **`050` is applied** (3 Sep 2026) — adds `user_mutes` and a symmetric enforcement check in `validate_session_participants()`, plus an auto-mute-on-report trigger on `reports` — see #49 and `supabase/migrations/README.md` (050).
2. **Check that a migration was actually applied, not just written** — `020_availability_schedule` sat unapplied for months. The old scheduled-availability route destructured only `data` from its query, so the "column does not exist" error was discarded and every run returned `{"notified": 0}` with a 200. The cron reported 1,000+ consecutive successes while the whole feature was inert. `supabase/migrations/README.md` now tracks what is applied; verify with `list_migrations` or by checking for the column, never by the file existing.
3. **Session creation is now validated in the database** — a `BEFORE INSERT` trigger on `sessions` requires the counterpart to be `available`/`always_available` (seeker-initiated) or `requesting` (listener-initiated), and rejects a session naming either participant as blocked. Rejecting only the *creator* as blocked (030's original version) missed the actual gap: a blocked user could still get connected to by someone else, since the counterpart side went unchecked. Migration 041 fixed that, added a trigger stopping a blocked user from setting `role_state`/`always_available` themselves, and fixed `/api/notifications/send` to check blocks too — see Known Issue #29 below and `supabase/migrations/README.md` (041). If a new connect path ever needs different rules, change `validate_session_participants()`, not just the client.
4. **`supabase/legacy/*.sql` is a stale snapshot** — policies edited in the Supabase dashboard never came back to the repo, so the files there do not describe the live database. Query `pg_policies` before reasoning about RLS (query in `supabase/migrations/README.md`).
5. **In-memory rate limiter** — `lib/rateLimit.ts` is shared by every route but its counters live in one serverless instance's memory, so they reset on cold start and aren't shared across instances. Move to Postgres or KV if it ever needs to be authoritative.
6. **SMS feature disabled** — all code written but commented out in `app/api/notifications/send/route.ts` and profile page (the unused `savingSms`/`smsSuccess`/`smsError` state and `handleSaveSms` are kept alongside it, with eslint-disables). Re-enable: uncomment both, add Twilio env vars, redeploy.
7. **Large page components** — admin (~1,840), chat (~1,720), profile (~1,400), dashboard (~1,270) lines, and still growing; extract components before major changes. The Broadcast tab was built as `components/BroadcastComposer.tsx` rather than more admin-page lines — keep doing that.
8. **Legacy blog tables** — migrations 011–015 create story tables no longer read by the app (stories moved to Ghost). The story email senders have been removed from `lib/email.ts`; the tables themselves are still there.
9. **Public repo** — the breach-response plan (marked "Internal — Do Not Publish") was removed from `docs/` in Aug 2026; it lives outside the repo now, so keep maintaining it there (the FTC Health Breach Notification Rule expects a written plan). Everything still in `docs/` is intentionally public: setup guides, plus dated audits whose findings are fixed. Note that git history still contains the removed file — the repo has always been public, so treat anything ever committed as disclosed.
10. **A direct connect opens the room before anyone is in it (superseded 29 Aug — see #28)** — tapping Connect on a listener creates the session immediately and notifies them *after*. The listener list admits anyone with a heartbeat inside the 1-hour window, so "available" can mean "was here 50 minutes ago". Measured 25 Aug: a seeker direct-connected, waited 84 seconds, left; the push had fired correctly 1 second after the session was created and the listener opened it 13 minutes later. Both sides behaved reasonably. The confirm dialog stated last-active and that a reply may take minutes, and the seeker saw a waiting note inviting them to write immediately. **29 Aug: that approach was replaced with a hard gate** — see #28 — after the same underlying problem was reported from the listener's side (two missed overnight requests) and reconsidered against `089148f`'s finding that 96 of 202 sessions were "seeker wrote, listener never replied." The confirm dialog's last-active transparency stays; the "write immediately" copy does not.
11. **Service worker cache** — `CACHE_NAME` in `public/sw.js` (currently v12); bump on breaking asset changes. A URL-only check for "is this client on a chat page" isn't the same as "is this client visible" — a stale or backgrounded chat tab matches the URL but the person isn't looking at it, so any suppression check in the push handler needs `isOnScreen(client)`, not just `client.url.includes(...)` (fixed 2 Sep 2026, see #33).
12. **Cron cadence is unreliable (worked around, not solved)** — GitHub Actions throttles the nominal `*/15` schedule. Measured 2026-08-24 over 30 consecutive runs: gaps of min 14m / median 21m / max 35m. `scheduled-availability` absorbs this with a wide match window plus a per-occurrence dedupe key. Anything else added to `cron.yml` must tolerate ~35 min between runs — don't assume 15.
13. **RLS-null embeds crash render (⚠️ gotcha when writing profile joins)** — Supabase relation embeds like `favorite_profile:profiles!fkey(...)` are *non-inner* joins, so row-level security returns the embedded object as **`null`** (it does not drop the parent row) whenever the viewer can't read that profile. The `profiles` SELECT policies expose a profile that is your own, currently `role_state = 'available'` or `'requesting'`, someone you share **any** session with (past or present, since migration 033), someone you have favourited, or anything at all if you're admin — so an embed only comes back null for a genuine stranger now. The guards stay: the null case is one policy change away from returning, and it fails as a render crash rather than a missing name. Dereferencing it (`fp.display_name`) throws during render and trips the error boundary ("Dashboard couldn't load"). **Always guard embedded profiles**: dashboard + profile favorites route through `lib/favorites.ts` `normalizeFavorites()`; profile thank-you notes, history, dashboard sessions, and admin all use `?.display_name || 'fallback'`. Use `!inner` only if you actually want RLS to filter the whole row instead.
14. **A client can only write its own profile row (⚠️ second RLS gotcha)** — `update(...)` against another user's row silently affects zero rows and returns **no error**. That's a footgun for anything that moves both chat participants at once; those transitions go through `/api/sessions/state` with the service role. Same shape of bug applies to any "delete my own X" that has no DELETE policy — a zero-row delete looks like success (this is what made self-service account deletion silently do nothing before `/api/account/delete` existed).
15. **`next` is pinned below 16** — postcss and sharp inside next 15 carried high advisories; both are pinned to patched versions via `overrides` in package.json. `npm audit` is clean, but revisit the overrides whenever next is upgraded.
16. **Logos still use `<img>`** — seven sites carry an eslint-disable rather than `next/image`, because the responsive `w-auto` sizing fights next/image's required dimensions. Worth converting deliberately for LCP.
17. **Stale `available` listeners: fixed who gets pushed to, and now the underlying rows correct themselves too** — `/api/notifications/send` used to target `role_state='available'` with no freshness check, so profiles hidden from every other list (they all filter on a 1-hour heartbeat) still received a push. Fixed 25 Aug: `lib/constants.ts` `isListenerOnline()` is now the single filter used by the send route, `AvailableListeners.tsx`, and `/listeners` — always_available bypasses the heartbeat (that's its whole point), a plain `role_state='available'` profile needs a fresh one within the last hour to be treated as online for *delivery*. Separately, `resetStaleAvailability()` in `cleanup-sessions` now runs on every cron tick and quietly flips `role_state` to `offline` for a plain-`available` profile whose heartbeat is **two weeks** stale or absent — deliberately a much longer window than the 1-hour delivery check, since this corrects the stored record rather than deciding a live push, and someone stepping away for an afternoon shouldn't come back to a status silently reset. `always_available` profiles are excluded from both mechanisms, on purpose, for the same reason (#23). Measured 25 Aug: 9 profiles reset on the first run, oldest heartbeat from February. The reset also queues a `listener_checkin` notification (off by default, migration 037) to exactly the profiles it just paused — telling them plainly and inviting them back; an admin has to switch it on. This superseded the old unused `cleanup_stale_availability()` DB function from migration 017 (that one had no `always_available` exception and a 5-minute threshold, which would have been wrong on both counts) — dropped outright by migration 045 rather than left in place. `notification_log` (migration 034) still holds historical send data if `supabase/queries/push_conversion.sql` is ever run against it.
18. **Admin accounts hide RLS bugs (⚠️ third RLS gotcha)** — the `is_admin()` SELECT policies let an admin read every profile, so a *missing* read policy looks perfectly fine when the owner tests it. Always check user-facing flows with a second, non-admin login. This is exactly how #1 survived in production. To test a policy without a second account, impersonate inside a rolled-back transaction: `set_config('request.jwt.claims', '{"sub":"<uuid>","role":"authenticated"}', true)` then `set local role authenticated` (full recipe in `supabase/migrations/README.md`).
19. **A SELECT policy is row-level, not column-level (⚠️ fourth RLS gotcha)** — every policy on `profiles` (available listeners, requesting seekers, favourited profiles, session participants, own row) grants the reader the *whole* row. Any column added to `profiles` is readable by everyone one of those policies already lets in, unless it's revoked separately. This is exactly how #1 (email/phone/consent/etc. readable by any signed-in user) survived in production — see migration 040, which revokes SELECT on the sensitive columns and adds `get_my_private_profile()` for a client to read its own back. Adding a new sensitive column to `profiles` means revoking it there too, not just trusting the row policy.
20. **A table-level GRANT beats a column-level REVOKE — they're additive, not layered (⚠️ fifth RLS gotcha, sort of — this one's plain Postgres privileges, not RLS)** — `authenticated`/`anon` had a blanket `grant select on table profiles` from day one (Supabase's default). Migration 040's first draft did `revoke select (email, phone_number, ...) on profiles from authenticated, anon` on top of that and shipped it live 2 Sep 2026 — it was a complete no-op. Postgres checks "does this role have table-level SELECT?" first; if yes, every column is readable and a narrower column-level revoke never gets consulted. Confirmed live: re-checked with the same impersonation recipe right after applying, and `email` still came back. The only way to actually restrict which columns a role can read is to revoke the table-level grant entirely and grant SELECT back on just the columns that should be visible — the reverse of what seemed intuitive. If a future migration needs to narrow column access on any table, start by checking `information_schema.role_table_grants` for an existing table-level grant; a column-level revoke against one is invisible, not just weaker.
21. **Queued notifications are only as timely as the cron** — `notification_queue` is drained by `/api/notifications/drain` on the shared 15-minute GitHub Actions schedule, which really means gaps of up to ~35 minutes (issue #11), plus up to 30 more minutes per quiet-hours deferral. That is by design — nothing in the queue is urgent — but it rules the queue out for anything that is. Support requests and chat messages stay on their direct paths for exactly this reason; don't move them.
22. **The queue drain now has a Vercel backup too** — `vercel.json` runs `/api/notifications/drain` daily at 5am UTC alongside `cleanup-sessions` (3am) and `scheduled-availability` (4am). If GitHub Actions ever stops (a rotated `CLEANUP_SECRET_KEY`, or GitHub disabling the schedule after a long gap in repo activity), queued notifications now get a once-daily flush instead of silently stalling and expiring after 3 days. The assumption this was blocked on — that Hobby caps cron jobs at two — turned out to be wrong: added a third and Vercel deployed it clean, no rejection. Watch the "Cron pings" GitHub Actions workflow for the real cadence; the Vercel crons are backup only, once a day.
23. **Listener supply is the real constraint, and two accounts dominate the silence (measured 25 Aug 2026)** — before optimising matching, know the shape of the pool. **Three** listeners have ever replied to anyone, all time; the same three in the last 30 days and the last 7. Only **22 of the last 720 hours** contained a single listener reply. Meanwhile two `always_available` accounts with push enabled account for **44 of the 96** sessions where someone wrote and got nothing back — `92aaf408` has sent **0 messages ever, in any role**, across 22 sessions, and `16b5c3e2` has sent 2 across 28. Both are trained. Both keep receiving support pushes because `always_available` deliberately bypasses the freshness check (see #17 — that's the toggle's whole purpose, not a bug), and until 25 Aug tapping that push created the session immediately via `/connect`, consuming the seeker before the listener had shown any intent to talk. **Do not tighten the direct-connect freshness window to fix this**: measured the same day, the list held 1 listener at the current 60-minute threshold and 0 at 30, 15 or 5 minutes. It is not a staleness problem. The levers that would actually help are (a) stop notifying accounts that never respond, (b) ~~make `/connect` confirm intent~~ **done 25 Aug** — a notification tap now only *offers* the connection; nothing is created until the listener says they're ready, so an idle tap no longer removes a seeker from the queue, (c) recruit listeners.
24. **"Connected" is not the same as "helped" (⚠️ the metric that was lying)** — a `sessions` row only means a listener tapped connect. Measured 25 Aug 2026 across 202 sessions: 48 real two-way conversations, 45 where nobody spoke, and **96 where the seeker wrote and the listener never replied**. Until that date the missed-connection follow-up excluded anyone with *a session*, so those 96 people were counted as helped, got no follow-up, and never appeared in the admin "Couldn't Connect" list. `lib/missedConnections.ts` now defines connected as *the listener actually replied*, and the cleanup cron sweeps recently-ended sessions as well as stale `requesting` seekers — the latter alone can never catch this, because ending a session moves the seeker to `offline`. **Any future query about whether people are being helped must count listener replies, not session rows.** The underlying question — why half of connects go unanswered — is still open; short session durations (median 98s unanswered vs 342s answered, 18 under 30s) suggest sessions ending before the listener can type, mechanism not yet identified.
25. **A broadcast can't be unsent** — `send_broadcast` writes a `user_notices` row per recipient immediately and queues the pushes, so by the time anyone notices a typo some of it has landed. There is no recall, and the composer says so before you confirm. If recall ever matters, the hook is deleting the broadcast's still-`pending` queue rows (the `user_notices` rows would need clearing separately).
26. **Ending a session outside `/api/sessions/state` left the listener stuck offline (fixed 28 Aug 2026)** — the cleanup cron, `/api/account/delete`, and admin `block_user`/`end_session` all closed sessions with a raw `sessions` update and nothing else, so only the chat page's own end-of-chat flow (which calls `/api/sessions/state`) ever moved the listener back to `available`. Anyone whose session was closed by one of those other paths — the common case for an actually-abandoned chat, which is exactly what the cleanup cron exists to catch — stayed at `role_state='offline'` indefinitely: silently dropped from `AvailableListeners`, `/listeners`, and every support-request push until they happened to reopen the dashboard and re-toggle "I'm Here To Listen". Extracted the transition into `lib/serverSessionState.ts` (`endSessionRoleStates()`, seeker → offline, listener → available) and call it from all four server routes, plus the dashboard's `endAllActiveSessions` (client-side, on sign-out) now goes through the existing `syncSessionRoleStates` helper per session it ends, the same way the chat page always has. `block_user` skips restoring the blocked user to `available` when they were the listener — the whole point of blocking them is that they shouldn't go back in the pool. Same pass also fixed two smaller ones: `NotificationSettings.tsx` now clears its success-message timers on unmount (a save immediately followed by navigating away could call `setState` on a gone component), and the admin broadcast preview's `countPushable()` now requires an actual `push_subscriptions` row, not just the announcements toggle, matching what the drain route really delivers — it was overstating reach for anyone who opted in but never registered a device.
27. **8 listeners in the notification pool cannot be reached at all — outreach needed** — as of 24 Aug 2026 the pool (`role_state='available'` OR `always_available`) holds 17 profiles: 6 always-available (5 reachable), 1 available-and-fresh (reachable), and 10 available-but-stale, of which **only 2 are reachable**. The other 8 have no push subscription and no `email_notifications_enabled`, so they receive nothing — item #17's targeting fix does not even apply to them, and they never appear in `notification_log`. They are phantom capacity inflating the apparent listener count. Not re-measured since; the underlying counts may have moved with #17's 2-week auto-reset now running. This is not a data question and no further measurement will resolve it on its own — item #17 established that push is what makes a listener useful, which makes these 8 exactly the people worth converting. A drafted outreach message for this group exists ("Group A") and, as of 29 Aug, still has not been sent. Coverage remains the platform's binding constraint, so this is the highest-value open item.
28. **A direct-connect session now requires the listener to accept before the seeker can message (29 Aug 2026)** — reported from the listener's side: two overnight direct-connect requests were missed entirely, and both seekers had already been able to send a message, which read as an active conversation being ignored rather than a request nobody had seen yet. This deliberately reverses part of `f05e80c`/`284e082` (#10, 25 Aug), which chose transparency-plus-encouragement over blocking, reasoning from a 12s median reply time among *answered* sessions. Revisited against the fuller number from the same audit (`089148f`, #24): 96 of 202 sessions were "seeker wrote, listener never replied" — the reassuring copy optimized for the responsive half while the larger half kept writing into silence. `sessions.accepted_at` (migration 039, applied) is NULL for a pending direct-connect session and defaults to `now()` for every listener-initiated path, since choosing to connect already is acceptance. A restrictive RLS policy on `messages` INSERT is the actual enforcement — the listener's own messages are never blocked, so replying still counts as accepting; only the seeker side is gated. `app/chat/[id]/page.tsx` hides the composer and starter prompts on both sides while pending and shows the listener an Accept / Not now prompt instead. The "write your message straight away" copy in `AvailableListeners.tsx`'s confirm modal and the in-chat waiting note that invited an immediate message were rewritten to stop contradicting the gate; the last-active transparency line from `284e082` stays, since it's orthogonal and still useful. The 84-second-abandon case `284e082` was measuring is now handled by "Cancel request" instead of by encouraging a message into a room that might stay empty. See `supabase/migrations/README.md` (039) for the verification detail.
29. **Blocking only stopped a user from starting a session, not from being reached (fixed 2 Sep 2026)** — `user_blocks` was checked in four client components before a session insert, all guarding *starting* one, and `validate_session_participants()` (030) only checked whether the trigger's *creator* was blocked. The dashboard's role buttons and `/api/notifications/send` didn't check at all. So a blocked user could press "I Need Support", broadcast to every listener, appear in People Seeking, and be connected to by any listener — the trigger passed because the listener wasn't the one blocked. Migration 041 added `is_user_blocked()`, fixed the trigger to check both participants, added a `profiles` trigger stopping a blocked user from setting `role_state`/`always_available` themselves, and `/api/notifications/send` now checks the caller's block status and, for the broadcast path, that they're actually `requesting` — the route had no rate-limit-independent gate on who could trigger a broadcast at all before this. Admin `block_user` also force-sets the blocked user's own `role_state` to `offline` immediately now, instead of only cleaning up sessions they were already in.
30. **Anyone signed in could overwrite or delete anyone else's avatar; anon could upload at all (fixed 2 Sep 2026)** — the live `storage.objects` policy "Authenticated users can manage avatars" was `FOR ALL` on `bucket_id = 'avatars'` with no ownership check, and three leftover template policies let the **anon** role INSERT/UPDATE jpg files under `avatars/public/`. Migration 042 drops all four and scopes INSERT/UPDATE/DELETE to `(storage.foldername(name))[1] = auth.uid()::text OR name LIKE auth.uid()::text || '-%'` — the OR keeps existing root-level `${userId}-${timestamp}.jpg` files owner-manageable while new uploads move to a `${userId}/${timestamp}.jpg` folder (`AvatarUpload.tsx`). Also set a 2 MB / jpeg+png+webp limit on `avatars` (wrong — undercut `AvatarUpload.tsx`'s existing 5 MB client-side check, so a 2-5 MB file passed client validation and then got silently rejected by storage; fixed same-day by migration 043 raising it to 5 MB), and drops `blog-images`'s open authenticated-upload policy (the blog moved to Ghost; public SELECT stays so old post URLs still resolve). See `supabase/migrations/README.md` (042, 043).
31. **A duplicate `reports` policy let the reported party read the report filed against them (fixed 2 Sep 2026)** — `public.reports` had two SELECT policies: "Users can view own reports" (`reporter_id = auth.uid()`, the one you want) and a duplicate, "Users can view their own reports" (`auth.uid() = reporter_id OR auth.uid() = reported_user_id`) — the OR let a reported user read `reporter_id` (who filed it), `description`, and `resolution_notes`. Nothing in `app/` reads reports as the reported party; every UI path is either the reporter's own view or the admin queue (`is_admin()`, separate policy). On a platform where people report harassment, that's a retaliation path. Migration 044 drops the wide policy, leaves the reporter-only one in place. See `supabase/migrations/README.md` (044).
32. **Connecting with a favorite never notified the listener (fixed 2 Sep 2026)** — `app/dashboard/page.tsx`'s `connectWithFavorite()` inserted the pending session and navigated straight to chat; unlike the other two direct-connect paths (`AvailableListeners.tsx`, `app/listeners/page.tsx`), it never called `/api/notifications/send` with `targetListenerId`. A favorited listener only found out if they happened to have the dashboard open for the next poll. Favorites are always-available, trusted listeners — exactly where the push matters most. Added the same fire-and-forget `/api/notifications/send` call the other two paths already make, right after the session insert succeeds, not awaited before `router.push`. This is a one-line patch, not the shared-connect-function refactor (three copies of the direct-connect flow still exist across `AvailableListeners.tsx`, `app/listeners/page.tsx`, and `app/dashboard/page.tsx`) — that consolidation is still open.
33. **A background or stale chat tab silenced every support-request push (fixed 2 Sep 2026)** — `public/sw.js`'s push handler suppressed support-request notifications (`seekerId` present) whenever any window client's URL matched `/chat/`, with no visibility check. A listener who left a tab open on a chat that had already ended, or had the PWA backgrounded on a chat page from an hour ago, got zero support pushes until they closed that tab — silently starving the notification pool. The very next check down (`clientList.some(isOnScreen)`) already handled the legitimate "actively looking at the app" case correctly; the URL-only `inChat` check was the bug. Now requires `isOnScreen(client)` on the chat window too, same as the `chat-message` suppression check above it. `CACHE_NAME` bumped to v12.
34. **A seeker got "was this helpful?" for a conversation that never started (fixed 2 Sep 2026)** — `app/chat/[id]/page.tsx`'s realtime `sessions` UPDATE handler opened the feedback modal whenever `status` flipped to `'ended'` and this client didn't trigger it, with no check on `accepted_at`. When a listener declined a pending direct-connect ("Not now"), the seeker cancelled ("Cancel request"), or the cleanup cron timed one out at 10 minutes, the other party — whoever was still sitting on the page — got the "was this conversation helpful?" modal and thank-you-note box, then a favorite prompt, for a chat that never had a single message. Now checks `accepted_at`: still-`null` means nothing happened yet for either side to rate, so a `pendingDeclined` bar replaces the modal in place — role-specific copy ("X cancelled the request" for the listener, "X isn't able to talk right now — try another listener or send a request to everyone" for the seeker) with a Back to dashboard button, same spot the pending-request bar occupied.

    Also fixed the role_state side of the same bug: `endSessionRoleStates()` (`lib/serverSessionState.ts`) always dropped the seeker to `'offline'` on session end, treating a declined pending request the same as a finished conversation. A seeker who reached out and got declined would silently vanish from People Seeking and every push target until they noticed and re-toggled — exactly the kind of stuck-state bug #26 fixed for listeners, just on the seeker side of an unaccepted session. Added a `wasAccepted` option (default `true`, so admin `block_user`/`end_session` and account deletion — which never had to make this distinction — are unaffected): when `false`, the seeker is restored to `'requesting'` instead, with a fresh heartbeat, so they stay visible and can act on the bar's own "try another listener or send a request to everyone" without re-clicking anything first. Wired from the two paths that can end a session still pending: `/api/sessions/state` (covers `cancelPendingSession()`, used for both decline and cancel) and `/api/cleanup-sessions` (the 10-minute timeout), both keyed off the session's own `accepted_at`, never trusted from the client.

37. **Revoking a function's EXECUTE from PUBLIC doesn't touch anon/authenticated — they get their own direct grant at CREATE time (⚠️ sixth RLS/privilege gotcha, again plain Postgres, not RLS — found 2 Sep 2026)** — this project's default privileges (`pg_default_acl`, role `postgres`) grant `EXECUTE ON FUNCTIONS` directly to `anon`, `authenticated`, and `service_role` on every function a migration creates, separate from whatever `PUBLIC` holds. Migration 041's `revoke execute on function is_user_blocked(uuid) from public` looked like it restricted the function to `authenticated` only — it didn't touch anon's access at all, because anon was never relying on the `PUBLIC` grant. Confirmed live via `information_schema.routine_privileges`: `anon` still had `EXECUTE` on `is_user_blocked` after that revoke ran. Same shape as #20 (table-level GRANT beats column-level REVOKE) one level up the privilege stack: additive grants from different sources don't get narrowed by revoking just one of them. Fixed by migration 045, which revokes `from anon, authenticated, public` explicitly wherever a function shouldn't be RPC-callable. If a future migration needs to lock a function down, revoke from every role that might hold an independent grant — checking `information_schema.routine_privileges` for the function name is the way to know which roles actually have it, the same way #20 said to check `role_table_grants` before assuming a REVOKE narrowed anything.
38. **Live DB drift and function hygiene (fixed 2 Sep 2026, migration 045)** — `protect_session_transitions()` (locks session participants/`created_at`, one-way ending, listener-only `accepted_at`) had been running in production with no migration file behind it; 045 adds it to the repo verbatim from the live definition. Six functions (`is_admin`, `restrict_message_update`, `handle_updated_at`, `update_updated_at_column`, `update_blog_posts_updated_at`, `cleanup_stale_availability`) were missing `search_path`, confirmed via the security advisor — the first five got it pinned, the sixth was dropped instead (see #17). Four `SECURITY DEFINER` trigger functions (`handle_new_user`, `protect_admin_flag`, `protect_session_transitions`, `validate_session_participants`) were callable as RPCs by anon and authenticated despite never needing to be — revoked, along with two more from migration 041 that had the same gap and weren't in the original review because they postdate it (`protect_blocked_role_state()`, `is_user_blocked(uuid)` — see #37 for why 041's own revoke on the latter didn't actually work). `is_admin()` is the one deliberate exception: several `public`-scoped RLS policies (on `profiles`, `sessions`, `messages`, `reports`, `user_blocks`) call it directly, so revoking anon's EXECUTE would turn anon-role reads against those tables into hard permission errors instead of a quiet "not admin" — left executable by both anon and authenticated. See `supabase/migrations/README.md` (045).
39. **Login redirect was an open redirect (fixed 3 Sep 2026)** — `app/login/page.tsx` took `?redirect=` straight from the URL and passed it to `router.push()` with no validation, on both the already-authenticated-user check and the post-login navigation. `router.push()` follows an absolute URL just as happily as a relative one, so `?redirect=https://evil.example` or the schemeless `?redirect=//evil.example` (protocol-relative) sent someone who'd just typed their password straight off the site — the realistic delivery is a phishing email with a genuine recoverybridge.app login link and a crafted `redirect` param. Fixed with a new `safeRedirectPath()` helper (`lib/safeRedirect.ts`, tests in `lib/safeRedirect.test.ts`): only a value starting with a single `/` and not `//` or `/\\` (some browsers normalize backslashes to forward slashes, so `/\\evil.com` is the same trick) passes through; everything else — absolute URLs, protocol-relative URLs, `javascript:`, empty/null — falls back to `/dashboard`. `app/signup/page.tsx` and `app/reset-password/page.tsx` have no equivalent `redirect` param; `app/connect/page.tsx`'s own redirect-to-login construction only ever builds a hardcoded internal path, so it needed no change.
40. **Duplicate RLS policies and per-row `auth.uid()` re-evaluation (written 3 Sep 2026, migration 046, NOT yet applied)** — the performance advisor reports 96 `multiple_permissive_policies` warnings and 43 `auth_rls_initplan` warnings. Two issues, same migration, no behaviour change either way. First: 13 policies across `sessions`, `messages`, `push_subscriptions`, and `user_blocks` are exact duplicates or strict subsets of another policy on the same table — dropped, keeping whichever name traces back to a legacy schema file or a numbered migration. Grepping for the dropped names turned up the same shape as #37/#38: none of them exist in `supabase/legacy/` or any migration file, meaning all 13 were created directly against the live database and never captured anywhere — untracked RLS drift, not just untracked functions. Second: every remaining policy on the nine tables the review named, plus `user_blocks` (flagged by the live advisor for the same reason, though the review's own list left it out), now wraps `auth.uid()` as `(select auth.uid())` so Postgres evaluates it once per query instead of once per row — invisible at 75 members, and cheaper to fix now than after the table grows. `admin_logs`, `blog_posts`, `broadcasts`, and the `notification_*` tables show the same `auth_rls_initplan` warning but were left alone — small admin-only tables where the per-row cost doesn't matter, and out of scope for this pass. See `supabase/migrations/README.md` (046).
41. **`scheduled-availability` dropped its stale-migration fallback and inline push loop (fixed 3 Sep 2026)** — `app/api/scheduled-availability/route.ts` still carried the "migration 027 not applied" fallback from when `last_availability_notify_key` didn't exist yet (027 has been live since 24 Aug). Keeping it meant a genuinely new missing-column error — the exact class of bug that let `020_availability_schedule` run silently broken for months (#2 above) — would have been swallowed by the same fallback instead of surfacing. Removed the fallback query, the `dedupeAvailable` flag, and made `last_availability_notify_key` non-optional on `ScheduleProfile`; a query error is a 500 now, same as every other route. Also replaced the route's own sequential `webpush.sendNotification` loop and manual `setVapidDetails` call with `fetchSubscriptionsByUser()` and `sendPushToSubscriptions()` from `lib/serverPush.ts` — the same helpers the cleanup cron's follow-up push and the notification queue drain already use. Two small behavior fixes came along with that: sends now go out in parallel instead of one at a time, and a stale subscription gets dropped on any 4xx (`lib/serverPush.ts`'s rule) instead of only on 404/410. Payload text, tag (`scheduled-availability`), and the mark-before-send dedupe are unchanged; `public/sw.js`'s push handler already reads a nested `data.data.url` before a top-level `data.url`, so the payload shape `sendPushToSubscriptions` builds (title/body/icon/tag plus `data: { url }`) needed no service-worker change.
42. **Admin delete-user left the counterpart mid-chat with a ghost (fixed 3 Sep 2026)** — `app/api/account/delete/route.ts` (self-service) ended the deleted user's active sessions and restored the other participant's `role_state` before deleting; `app/api/admin/delete-user/route.ts` just called `auth.admin.deleteUser()` directly, so an admin deleting someone mid-chat left the other person's session dangling and their `role_state` stuck until the cleanup cron's 10-minute timeout caught it — same shape of bug as #35/#26, just triggered by an admin action instead of a client tap. Extracted the shared behavior into `lib/deleteUserAccount.ts`'s `deleteUserAccount(supabaseAdmin, userId)` — ends any active session the user is in, runs `endSessionRoleStates()` for each, then deletes the auth user; the caller supplies its own service-role client and has already handled auth, this does no permission checking of its own. Both routes now call it. Also added `targetUserId` UUID validation to the admin route, same regex and same reasoning as `block_user`'s `userId` check in `app/api/admin/actions/route.ts` — it gets interpolated into a PostgREST `.or()` filter inside `deleteUserAccount()`. The regex moved to `lib/validation.ts` as `UUID_RE` so both routes import the same one instead of `app/api/admin/actions/route.ts` keeping its own private copy.

43. **Three seeker-side direct-connect flows had drifted; two never synced role state (fixed 3 Sep 2026)** — `components/AvailableListeners.tsx`, `app/listeners/page.tsx`, and `app/dashboard/page.tsx`'s `connectWithFavorite` each had their own ~50-line copy of "block check → create session → notify → mark offline." They'd drifted apart in a way that mattered, not just cosmetically: `app/listeners/page.tsx` never checked for an existing session before inserting, and neither it nor `connectWithFavorite` ever called `syncSessionRoleStates(supabase, session.id, 'start')` — so a seeker connecting through either of those two paths stayed `requesting` (visible to every other listener, poll-refreshed onto other people's lists) for the entire time their request sat pending, instead of moving to `offline` like the `AvailableListeners.tsx` path already did correctly. Consolidated all three into `lib/directConnect.ts`'s `startDirectConnect(supabase, { seekerId, listenerId })` (tests in `lib/directConnect.test.ts`): block check, existing-session rejoin, insert with `accepted_at: null`, fallback-to-newest-session on an insert race, fire-and-forget "direct connect" push, then the role_state sync — all three call sites now behave identically, and the two missing-sync bugs are just gone. `connectWithFavorite` also dropped its own duplicate notify-fetch, since `startDirectConnect` already sends one.

44. **Consent and training completion were client-written (fixed 3 Sep 2026, migration 047)** — `app/signup/page.tsx` puts `consent_version`, `consent_accepted_at`, `age_confirmed`, `health_data_consent`, and `health_data_consent_at` into auth signup metadata, and `app/onboarding/page.tsx` copied those same client-controlled values into `profiles` with a plain `UPDATE` — anyone could open the console and backdate their own consent record. `handle_new_user()` now copies the consent fields at profile creation instead, with `consent_accepted_at` and `health_data_consent_at` generated server-side (`now()`); the boolean/version values still come from signup metadata since signup is the one place consent is actually granted and there's no server-side source of truth for *what* was agreed to, only *when*. `app/onboarding/page.tsx` no longer writes any of the five fields. Separately, `app/training/page.tsx` wrote `listener_training_completed_at` directly from the client with nothing checking the eight training sections were actually acknowledged — a new `protect_training_completion` trigger (same skip-condition shape as `protect_admin_flag` #028 and `protect_blocked_role_state` #041) only lets it move from `NULL` to non-null when `listener_training_progress` shows every id in `lib/constants.ts`'s `LISTENER_TRAINING_SECTION_IDS` acknowledged, and refuses to let it change again once set. `app/onboarding/page.tsx`'s own listener path sets the same column as an onboarding step but had never persisted `listener_training_progress` to the DB before this (it only lived in local state) — fixed by writing both columns in the same `UPDATE` so the trigger's `NEW` row sees them together. All four trigger behaviors (partial progress rejected, full progress accepted, re-completion rejected, unrelated edit passes through) confirmed against production in rolled-back transactions. See `supabase/migrations/README.md` (047).

45. **No security headers, and no CSP at all (fixed 3 Sep 2026)** — `next.config.js` had no `headers()`. Vercel adds HSTS by default but nothing else. Added `X-Content-Type-Options: nosniff`, `Referrer-Policy: strict-origin-when-cross-origin`, `X-Frame-Options: DENY`, and `Permissions-Policy: camera=(), microphone=(), geolocation=()` on every route, plus a `Content-Security-Policy-Report-Only` header — **report-only, not enforcing**, since a full CSP touches Sentry, Supabase realtime, and the site's own inline JSON-LD, and getting one of those wrong should show up in reports before it breaks the app. Supabase's host and Sentry's ingest host are read from `NEXT_PUBLIC_SUPABASE_URL` and `NEXT_PUBLIC_SENTRY_DSN` at config-load time rather than hardcoded, so the same file works across environments; a missing or malformed value just drops that source instead of failing the build. `script-src` and `style-src` need `'unsafe-inline'` — the JSON-LD `<script type="application/ld+json">` tags on the landing page and every `app/support/*/page.tsx` are still `<script>` elements as far as CSP is concerned even though they never execute as JS, and Tailwind relies on inline styles in places. `img-src` allows `blob:` for `AvatarUpload.tsx`'s `URL.createObjectURL()` preview. Sentry's `tunnelRoute: '/monitoring'` (already configured) proxies most client Sentry traffic same-origin, but Session Replay can still talk to the ingest host directly, so it's allowed too. **To promote to enforcing:** watch Sentry's report-only violations (or a real browser's devtools console on a few page loads) for a while first — the moment nothing's getting flagged, rename the header from `Content-Security-Policy-Report-Only` to `Content-Security-Policy`.

46. **Tracked junk in the repo (fixed 3 Sep 2026)** — `.claude/launch.json`, `.clone/worktrees/inspiring-morse-038152/app/training-preview/page.tsx`, and `.~lock.RecoveryBridge_Demo.pdf#` were committed to git despite `.claude/` and `.clone/` already being in `.gitignore` (added before those specific files were tracked — `.gitignore` only stops *new* files from being added, it doesn't retroactively untrack existing ones). Ran `git rm --cached` on all three so they drop out of the repo on the next commit while staying on disk. Added `.~lock.*` and `*.pdf#` to `.gitignore` so a stray LibreOffice lock file can't get re-added by accident. `scripts/get-user-emails.js` (dumped every member's email to stdout using the service role key — no reason to exist now that the admin page has an email export) is deleted. `node_modules` still shows the iCloud/Dropbox-sync duplication the review flagged (`@babel 2`, `@eslint 2`, etc. — 346 duplicated top-level folders as of this check) but `lib.dom.d.ts` is present and `tsc`/`eslint` have run clean through this whole review, so nothing is actively broken by it right now; left alone rather than doing a destructive `rm -rf node_modules && npm ci` on a working tree without asking first — worth doing at some point regardless, since it is what caused the review's own `npx tsc` run to fail to find `lib.dom.d.ts` in the first place.

47. **Dead route, dead columns, dead SMS feature (fixed 3 Sep 2026)** — three separate cleanups from the review's item 21. `app/api/webhooks/new-user/route.ts` is superseded by `app/api/admin/notify-signup/route.ts` (called from onboarding completion) — confirmed via direct SQL against production rather than the Supabase dashboard: `pg_trigger` shows no trigger on `public.profiles` calling `supabase_functions.http_request()` (i.e. no live database webhook of any kind right now), and `supabase_functions.hooks` shows the only database webhook that ever fired against `profiles` was named `notify-signup`, last fired 2 Jul 2026 — never `new-user`. So nothing points at this route and it's safe to delete — deleted, and `ADMIN_NOTIFICATION_EMAIL`/`SUPABASE_WEBHOOK_SECRET` removed from `.env.example` with it. `profiles.bubble_user_id` and `profiles.is_suspended` (nothing in the app reads either — confirmed by grep) got folded into the still-unapplied `038_drop_unused_schema.sql` alongside its existing `requesting_since`/`blocks` drops, with the same verify-empty guard pattern extended to cover both. `lib/sms.ts` (the Twilio integration that's been "pending verification" with its calling code commented out in `app/api/notifications/send/route.ts` since before this file's git history starts) got a dated TODO instead of deletion, per the review's own call — remove it on 1 Dec 2026 if Twilio still isn't verified by then. See `supabase/migrations/README.md` (038, extended).

48. **Missing FK indexes (fixed 3 Sep 2026, migration 049)** — the review's item 23 named 5 columns to index from the advisor's list of 16 uncovered FKs. Checked all 16 against actual query code instead of taking the review's list at face value: `messages.sender_id`, `reports.reporter_id`, and `session_feedback.from_user_id` each back a real `.eq()` filter in `app/api/account/export/route.ts` (the account-data-export endpoint); `reports.reported_user_id` backs the admin page's `profiles!reports_reported_user_id_fkey` embed join. All four got a plain b-tree index. `user_notices.created_by` — the fifth column the review named — turned out to already be covered: nothing filters by it, and `idx_user_notices_user_created (user_id, created_at)` plus `idx_user_notices_kind_created (kind, created_at)` already serve every real query against that table (`NoticeBanner`, the admin missed-connections list, the reconnect-notice dedupe in the cleanup cron). Adding an index there would have had no query to serve, so it's skipped. The other 6 uncovered FKs the review didn't mention (`broadcasts.created_by`, `notification_kind_settings.updated_by`, `notification_log.seeker_id`, `user_blocks.blocked_by`, `reports.resolved_by`, `reports.session_id`) got the same check and came up empty too — same bucket as `admin_logs`/`blog_posts`, deferred for the same reason. The 6 indexes the review flagged as unused (`idx_messages_read_at`, `idx_reports_status`, `idx_notification_queue_broadcast`, `idx_admin_logs_created_at`, `idx_notification_log_listener_created`, `blog_posts_tags_idx`) all still show `idx_scan = 0` as of 3 Sep — not dropped yet, re-check `pg_stat_user_indexes` around 3 Dec 2026 before dropping. See `supabase/migrations/README.md` (049).

49. **User-to-user muting (fixed 3 Sep 2026, migration 050)** — the review's item 24: `user_blocks` is admin-only and platform-wide, so a seeker who had a bad session had no way to avoid being matched with that same listener again short of filing a report and waiting on you, and a listener had no way to keep a specific seeker from direct-connecting. New `user_mutes(muter_id, muted_id, session_id, source)`, modeled directly on `user_favorites` (007) — same "you can only act on someone you've actually had a session with" RLS gate, same table shape. Three differences from favorites: enforcement is symmetric (it's checked both directions in `validate_session_participants()`, the same trigger that already blocks a restricted account from starting a session — doesn't matter who muted whom, neither side can reach the other once a mute exists); there's no delete policy yet, so nobody but a service-role/admin action can undo one (no unmute UI in this first version, by design); and filing a report auto-creates a mute (`source='report'`) via a new `AFTER INSERT` trigger on `reports`, so a reporter isn't left re-matchable with someone while their report sits unresolved. It's silent both ways — nobody is ever told a mute happened, on either side; each party's own client just quietly stops showing the other (`Users can view mutes involving themselves` policy lets each side's list filter correctly without either learning why). List filtering added to People Seeking, Available Listeners, the listeners page, and the dashboard favorites list (UX only — the trigger is the actual wall); the broadcast notify route (`app/api/notifications/send/route.ts`) also excludes a muted listener from who gets paged, which is the one filtering point that matters on its own since a broadcast never goes through session creation at all. The post-chat modal has a new "Don't match me with this person again" option — deliberately a small low-key link, not a button, and it's a two-step confirm (click reveals a second "are you sure" screen before anything is written) so it can't be tapped by accident or in the heat of the moment. Admin gets a "muted by N people" badge on the reports view (`is_admin()`-gated `Admins can view all mutes` policy) when N > 1 — deliberately not shown at N=1, since every report already creates one mute of its own, so a count of 1 is just the report restating itself; 2+ means someone besides the reporter independently muted the same person too. All of it (manual mute succeeds with a real shared session, blocked both directions once it exists, report auto-mute fires, self-mute rejected, muting a stranger with no shared session rejected, admin sees all / everyone else sees only their own) verified against production in rolled-back transactions. See `supabase/migrations/README.md` (050).

50. **API route authorization tests (fixed 3 Sep 2026)** — the review's item 22: three routes that make real authorization decisions (`app/api/sessions/state/route.ts`, `app/api/notifications/send/route.ts`, `app/api/admin/actions/route.ts`) had no test coverage at all — every other `.test.ts` in the repo covers a `lib/` helper, none construct a `NextRequest` or exercise a route handler directly. New `lib/test/fakeSupabase.ts`, shared by all three: unlike the single-generic-chain fakes in `lib/directConnect.test.ts`/`lib/notificationQueue.test.ts` (fine for a helper that touches one table), these routes hit several different tables per request, sometimes the same table twice expecting different results — so this one dispatches by table name, with an array-per-table option for the "queried twice" case, and every chain object is itself thenable so `await` resolves correctly no matter where in the chain (`.select()`, `.eq().single()`, `.eq().maybeSingle()`) the route happens to land. It also records every call (`calls`), so a test can assert what was actually queried, not just the response — used to prove `sessions/state`'s `'start'` phase touches only the seeker's `profiles` row, not the listener's, when a direct connect is still pending (see #35's own fix). 10 tests total: `sessions/state` (401 no auth header, 403 non-participant, 409 ending an already-active session, the seeker-only update on a pending start); `notifications/send` (403 broadcast from a non-`'requesting'` seeker, 403 direct-connect with no active session, 403 a blocked seeker); `admin/actions` (403 non-admin, 400 unknown action, and 409 — not 400 or 403 — sending a broadcast while announcements are switched off, which the review's own prose reads like a validation failure but isn't). `createClient` from `@supabase/supabase-js` is mocked per test file to return a `fakeSupabase()` client; `web-push` is mocked outright since `setVapidDetails()` validates VAPID key format and a placeholder test key would throw before any assertion ran. `npx tsc --noEmit` and `npx eslint .` both clean; `npm test` itself can't run from this sandbox (no Linux build of the native module `node_modules` needs) — Burk ran it locally: 129 tests, all passed (up from 119 before this item).

51. **An abandoned branch had already patched production directly, undocumented (found 4 Sep 2026)** — while resolving an unrelated push conflict, found `claude/recovery-bridge-code-review-1zlgj0` on GitHub (last commit 31 Aug 2026, never merged to main). That session had its own Supabase access and used it: two real RLS/trigger fixes were applied straight to production, bypassing the "Burk pastes the SQL himself" workflow this file assumes throughout. Confirmed both still live by querying production directly (not by trusting the branch's commit messages). Migrations 051 and 052 backfill the history to match — see `supabase/migrations/README.md`. The branch also contained three application-code fixes (chat-page accept-gate deadlock, wrong feedback modal on a declined request, dead requests cluttering `/history`) that were never deployed, since that only happens through a normal merge to `main` — those are folded into this review as items 52-54, implemented fresh against the current codebase rather than copied from the old branch. The branch itself is confirmed dead: Burk never ran anything from it by hand, and nothing else on it is unaccounted for.

52. **A dropped realtime event could deadlock a pending direct connect (fixed 4 Sep 2026)** — from the abandoned branch (see #51), reimplemented fresh. Chat has had a 3s message poll since a dropped realtime INSERT was found to lose messages, but the session row had no equivalent fallback — `loadSession()` runs once on mount and the realtime UPDATE was the only way this side learned `accepted_at` had been set. Survivable when the row only carried `status`; after migration 039 it gates the seeker's composer, so a dropped event left them stuck on "Waiting for X to accept..." unable to reply while the listener was already typing at them, with no recourse short of a manual refresh. New poll, same shape as the message one, active only while `status === 'active' && !accepted_at` (matches `isPendingAcceptance`) so an ordinary already-accepted conversation pays nothing for it. Both the realtime handler and the new poll now go through one `applySessionUpdate()` — extracted, not duplicated, so the two paths can't drift on what an ended-while-pending session means.

53. **A declined request wrongly asked the seeker to rate it — already fixed, confirmed still correct.** The abandoned branch (#51) flagged this too, but the current codebase already carries the fix: `pendingDeclined` state, set whenever a session ends with `accepted_at` still null, replaces the "was this conversation helpful?" feedback modal with a short "request wasn't accepted" bar instead. No change needed here — verified it's live in both the realtime path and the new poll (#52) added above, since both now route through the same `applySessionUpdate()`.

54. **Never-accepted direct connects cluttered /history (fixed 4 Sep 2026)** — from the abandoned branch (see #51). A direct connect the listener declined, or that the cleanup cron timed out at 10 minutes, ends with `status='ended'` and `accepted_at` still null — no transcript, nothing that happened for either side to rate. `/history`'s query only filtered on `status='ended'`, so these showed up as 0-message "conversations" with the other person's name and avatar on them, indistinguishable from a real chat until you opened it. Added `.not('accepted_at', 'is', null)` to the query. The admin sessions tab is untouched on purpose — that view is the raw record, and a moderator wants to see the requests that died too, same reasoning as the admin visibility calls in #29/#49.
    Did the same for the listener-accepting-a-seeker side: `components/PeopleSeeking.tsx`'s `connectWithSeeker` and `app/connect/page.tsx`'s `accept()` shared the same block-check-then-insert-then-sync shape but weren't drifted in any behavior-changing way, just duplicated. Extracted the shared part into `lib/acceptSeeker.ts`'s `acceptSeeker(supabase, { listenerId, seekerId })` (tests in `lib/acceptSeeker.test.ts`), returning a 4-way result (`session` / `blocked` / `conflict` on the unique-violation / `error`). Deliberately left each page's own logic in place rather than folding it in too: `PeopleSeeking.tsx` still does its own upfront `role_state === 'requesting'` re-check before calling `acceptSeeker` (avoids a pointless round trip when its own list is stale — the DB's `validate_session_participants()` trigger enforces the real guard either way), and `app/connect/page.tsx`'s `check()` preflight and `describeUnavailable()` failure-readback stay as they were — genuinely page-specific UX, not shared behavior.

36. **`/api/cleanup-sessions` accepted any signed-in user's token, no rate limit (fixed 2 Sep 2026)** — the route's auth block had a third branch beyond the two cron secrets: any bearer token that passed `supabase.auth.getUser()` was let through, and `app/dashboard/page.tsx` called it on every page load with the viewer's own token. Since the GitHub Actions cron has run reliably for months (1,000+ consecutive runs), that path was a leftover, not a real caller — but it meant any account could trigger a full sweep (every active session and its messages read, three profile UPDATEs, notice inserts, pushes) on demand, with nothing to stop them hammering it. Replaced the hand-rolled block with `isAuthorizedCronRequest()` from `lib/cronAuth.ts` (already shared by every other cron route) called once for `x-cleanup-secret` and once for the default `x-cron-secret`/`Authorization: Bearer` — the "any authenticated user" branch is gone entirely. Deleted `cleanupStaleSessions()` and its mount-effect call from the dashboard. Also collapsed the route's two near-identical code paths (an early return for "no active sessions" used to duplicate the stale-requesting reset, the missed-connection follow-up, and the availability reset) into one flow: session-closing logic is now skipped, not the rest of the sweep, when there's nothing to close, and there's a single response shape either way.

35. **A seeker's tap took the listener offline before they'd done anything (fixed 2 Sep 2026)** — `AvailableListeners.tsx`, `/listeners`, and `connectWithFavorite()` all called `syncSessionRoleStates(session.id, 'start')` immediately after inserting a pending direct-connect session, and `/api/sessions/state`'s `'start'` phase moved both participants to `'offline'` unconditionally. So a single seeker tap removed the listener from every list and every broadcast push for up to 10 minutes — until they accepted, declined, or the cleanup cron closed it — without the listener having accepted anything, or even necessarily seen the request yet. Combined with migration 029's one-active-session-per-listener index, it also blocked any other seeker from reaching that listener for the same window. `'start'` now only moves the seeker when `accepted_at` is still null; the listener stays exactly where they were. `acceptConnection()` in the chat page fires its own `syncSessionRoleStates(sessionId, 'start')` right after the `accepted_at` update succeeds — that's the moment the listener actually leaves the pool now, not the seeker's original tap. `'end'` was already unconditional (`endSessionRoleStates()`, #26) so declining, cancelling, or timing out still needs no special case there — a listener who was never moved offline is simply set to `'available'` again, a no-op. The 029 index is untouched: a pending session still blocks a second direct-connect to the same listener, which is fine since the cleanup cron clears an unanswered one after 10 minutes.





---

## Quality Gates

All of these are clean as of August 2026 — keep them that way:

```
npm test          # vitest — 129 tests across constants, favorites, timeWindows, rateLimit, errors, sessionState, missedConnections, notificationQueue, directConnect, acceptSeeker, safeRedirect, and the sessions/state, notifications/send, admin/actions route tests
npx tsc --noEmit  # no type errors
npx eslint .      # 0 errors, 0 warnings
npm audit         # 0 vulnerabilities
npm run build     # succeeds with NO server secrets set (clients are built per-request, never at module scope)
```

That last point matters: never construct a Resend or service-role Supabase
client at module scope in a route. Next imports every route during the build's
page-data collection, and a constructor that throws on a missing key turns one
absent env var into a failed deploy for the whole site.

Effects with deliberately trimmed dependency arrays carry an explicit
`// eslint-disable-next-line react-hooks/exhaustive-deps -- <reason>`. Keep that
convention so a genuinely new warning stands out instead of joining a pile.

---

## Git & Deploy Workflow

- **`main` auto-deploys to production** on Vercel (project `recovery-bridge`, team `burkjacksons-projects`)
- **CI runs on every push** (`.github/workflows/ci.yml`): typecheck → lint → test → build. It needs no secrets — API routes construct their Resend/Supabase clients per request, so keep them out of module scope or the build breaks in CI and on Vercel alike
- Solo project: commit to `main` directly or merge worktree branches to main — **never open PRs**
- Domains: recoverybridge.app (+www), stories.recoverybridge.app (Ghost)

---

## Working Notes (preferences from Burk)

- **Email drafts for manual sending (Gmail):** write plain linear text only — short paragraphs, one instruction per line, full URLs written out. No Markdown structure (bold markers, numbered lists with inline links, arrows, tables); it collapses into unreadable columns when pasted into Gmail.
